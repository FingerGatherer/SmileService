<Window x:Class="SmileService.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Управление сервисным центром «Смайл»" 
        Height="700" Width="1150"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="#333333"
        Background="#F4F6F9"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E5E8ED" BorderThickness="0,0,0,1">
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBox x:Name="SearchTextBox" Width="220" 
                             materialDesign:HintAssist.Hint="Поиск заявки..." 
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Height="40" Padding="10,8"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="BtnHome" Style="{StaticResource MaterialDesignFlatButton}" Content="Главная" Click="MenuButton_Click" Foreground="#555555" Margin="5,0"/>
                    <Button x:Name="BtnClients" Style="{StaticResource MaterialDesignFlatButton}" Content="Клиенты" Click="MenuButton_Click" Foreground="#555555" Margin="5,0"/>
                    <Button x:Name="BtnOrders" Style="{StaticResource MaterialDesignFlatButton}" Content="Заявки" Click="MenuButton_Click" Foreground="#1976D2" FontWeight="Bold" Margin="5,0"/>
                    <Button x:Name="BtnUsers" Style="{StaticResource MaterialDesignFlatButton}" Content="Сотрудники" Click="MenuButton_Click" Foreground="#555555" Margin="5,0"/>
                    <Button x:Name="BtnReports" Style="{StaticResource MaterialDesignFlatButton}" Content="Отчеты" Click="MenuButton_Click" Foreground="#555555" Margin="5,0"/>
                    <Button x:Name="BtnWarehouse" Style="{StaticResource MaterialDesignFlatButton}" Content="Склад" Click="BtnWarehouse_Click" Foreground="#555555" Margin="5,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,5,0">
                    <TextBlock x:Name="TxtUserName" Text="ФИО Пользователя" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="#333333" FontWeight="SemiBold"/>
                    <Button x:Name="BtnLogout" 
                            Content="Выйти" 
                            Click="BtnLogout_Click"
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Foreground="#D32F2F" 
                            FontWeight="SemiBold"
                            Padding="8,4"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <Frame Grid.Row="1" x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>