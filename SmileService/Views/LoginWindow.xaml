<Window x:Class="SmileService.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Авторизация — Смайл" Height="400" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F5F7FB" WindowStyle="None" AllowsTransparency="True">

    <Border CornerRadius="12" Background="White" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="3" Opacity="0.15"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,10,0,25" HorizontalAlignment="Center">
                <TextBlock Text="SmileService" FontSize="26" FontWeight="Bold" Foreground="#1976D2" HorizontalAlignment="Center"/>
                <TextBlock Text="Вход в систему учета" FontSize="12" Foreground="#888888" Margin="0,4,0,0" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="Логин" FontSize="12" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,6"/>
                <TextBox x:Name="TxtLogin" Height="38" Padding="10,5" VerticalContentAlignment="Center" FontSize="14"
                         Background="#F9FAFC" BorderBrush="#E2E8F0" BorderThickness="1"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,25">
                <TextBlock Text="Пароль" FontSize="12" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,6"/>
                <PasswordBox x:Name="TxtPassword" Height="38" Padding="10,5" VerticalContentAlignment="Center" FontSize="14"
                             Background="#F9FAFC" BorderBrush="#E2E8F0" BorderThickness="1"/>
            </StackPanel>

            <StackPanel Grid.Row="4">
                <Button Content="Войти" Height="42" Background="#1976D2" Foreground="White" FontWeight="Bold" FontSize="14"
                        Click="BtnLogin_Click" IsDefault="True" Cursor="Hand"/>

                <Button Content="Закрыть" Height="30" Margin="0,10,0,0" Background="Transparent" Foreground="#888888" BorderThickness="0"
                        Click="BtnCancel_Click" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>