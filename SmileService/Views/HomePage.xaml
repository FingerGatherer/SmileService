<Page x:Class="SmileService.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="930"
      Title="HomePage" Loaded="Page_Loaded">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Панель управления СЦ «Смайл»" FontSize="24" FontWeight="Bold" Foreground="#222222" Margin="0,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <Border Background="White" CornerRadius="8" Margin="0,0,15,0" Padding="20" BorderBrush="#E5E8ED" BorderThickness="1">
                <StackPanel>
                    <materialDesign:PackIcon Kind="ProgressWrench" Foreground="#1976D2" Width="32" Height="32"/>
                    <TextBlock Text="В работе" FontSize="14" Foreground="#777777" Margin="0,10,0,5"/>
                    <TextBlock x:Name="TxtActiveOrders" Text="12 заявок" FontSize="22" FontWeight="Bold" Foreground="#222222"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="8" Margin="10,0,10,0" Padding="20" BorderBrush="#E5E8ED" BorderThickness="1">
                <StackPanel>
                    <materialDesign:PackIcon Kind="NewBox" Foreground="#2E7D32" Width="32" Height="32"/>
                    <TextBlock Text="Новые за сегодня" FontSize="14" Foreground="#777777" Margin="0,10,0,5"/>
                    <TextBlock x:Name="TxtNewOrders" Text="4 заявки" FontSize="22" FontWeight="Bold" Foreground="#222222"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="8" Margin="15,0,0,0" Padding="20" BorderBrush="#E5E8ED" BorderThickness="1">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CurrencyRub" Foreground="#E65100" Width="32" Height="32"/>
                    <TextBlock Text="Выручка (текущий месяц)" FontSize="14" Foreground="#777777" Margin="0,10,0,5"/>
                    <TextBlock x:Name="TxtRevenue" Text="84,500 ₽" FontSize="22" FontWeight="Bold" Foreground="#222222"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20" BorderBrush="#E5E8ED" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Оперативная сводка и уведомления" FontSize="16" FontWeight="Bold" Foreground="#222222" Margin="0,0,0,10"/>
                <TextBlock Text="• Проверьте новые заявки, распределенные автоматической очередью." Foreground="#555555" Margin="0,5"/>
                <TextBlock Text="• Требуется закупка запчастей по 3 заказам (см. модуль 'Склад')." Foreground="#555555" Margin="0,5"/>
                <TextBlock Text="• Системное время и статус подключения к БД: Стабильно." Foreground="#2E7D32" Margin="0,5"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>