<Page x:Class="SmileService.Views.WarehousePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Управление складом" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Складской учет запчастей" FontSize="22" FontWeight="Bold" Foreground="#222222" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Название детали" FontSize="11" Margin="0,0,0,4" Foreground="Gray"/>
                    <TextBox x:Name="TxtName" Width="180" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Артикул (SKU)" FontSize="11" Margin="0,0,0,4" Foreground="Gray"/>
                    <TextBox x:Name="TxtSku" Width="100" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Цена закупки" FontSize="11" Margin="0,0,0,4" Foreground="Gray"/>
                    <TextBox x:Name="TxtPurchase" Width="90" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Цена продажи" FontSize="11" Margin="0,0,0,4" Foreground="Gray"/>
                    <TextBox x:Name="TxtSelling" Width="90" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Кол-во" FontSize="11" Margin="0,0,0,4" Foreground="Gray"/>
                    <TextBox x:Name="TxtQty" Width="60" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <Button Content="➕ Добавить" Background="#FF2E7D32" Foreground="White" Width="124" Height="32" Margin="0,15,10,0" FontWeight="Bold" Click="BtnAdd_Click"/>
                <Button Content="➖ Списать 1 шт." Background="#C62828" Foreground="White" Width="110" Height="32" Margin="0,15,0,0" FontWeight="Bold" Click="BtnWriteOff_Click"/>
            </WrapPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="PartsGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White" RowHeight="35"
                  SelectionChanged="PartsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Наименование запчасти" Binding="{Binding PartName}" Width="*"/>
                <DataGridTextColumn Header="Артикул (SKU)" Binding="{Binding Sku}" Width="120"/>
                <DataGridTextColumn Header="Закупка (руб.)" Binding="{Binding PurchasePrice, StringFormat={}{0:N2}}" Width="110"/>
                <DataGridTextColumn Header="Продажа (руб.)" Binding="{Binding SellingPrice, StringFormat={}{0:N2}}" Width="110"/>
                <DataGridTextColumn Header="Остаток (шт.)" Binding="{Binding Quantity}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Quantity}" Value="0">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>